#!/usr/bin/env bash

if [[ "$#" -ne 1 ]]; then
	echo "Usage: sshu-getpub \"name\""
	exit 1
fi

name="$1"

SSH_DIR="/home/sshu/.ssh"
KEY="$SSH_DIR/$name"
CONFIG="$SSH_DIR/config"

if [[ ! -f "$KEY" ]]; then
	echo "Key \"$name\" does not exist"
	exit 1
fi

ssh-keygen -y "$KEY"
